server:
  port: 8080

spring:
  application:
    name: biodata-service

  jpa:
    database: MYSQL
    show-sql: false
    hibernate:
      ddl-auto: update
    open-in-view: false

  datasource:
    url: jdbc:mysql://localhost:3306/biodata_service_db
    username: root
    password: root